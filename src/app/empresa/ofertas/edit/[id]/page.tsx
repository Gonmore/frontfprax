'use client';

import EmpresaOfertasNewPage from '../../new/page';

export default function EmpresaOfertasEditPage() {
  // El componente EmpresaOfertasNewPage detecta automáticamente si está en modo edición
  // revisando la URL del navegador
  return <EmpresaOfertasNewPage />;
}